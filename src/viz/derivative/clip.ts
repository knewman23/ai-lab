/**
 * The display box every line in the derivative scene is clipped to: the
 * tangent, the secant and the zoomed curve all stop here, clear of the
 * derivative band.
 */
export const CLIP: readonly [number, number] = [3.5, 3.4];
